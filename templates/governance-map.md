<!-- Written into a new product's AGENTS.md as its Governance Map (the CLI's `init` command
     does this automatically — writes the file fresh, or safe-appends this block if an
     AGENTS.md already exists without a "Governance Map" section). This is the entry-point
     index audit-structure and compile-prompt read first; keep it current as the product's
     L2 homes grow past this single file. -->

# Governance Map

- **Constitution (L0/L1)**: `CONSTITUTION.md`
- **Case Law (L3)**: `decisions/`
- **Statutes (L2)**: Managed in this `AGENTS.md` file (or specify other files/globs here).

*This file serves as the entry-point index for the audit-structure and compile-prompt skills.*
