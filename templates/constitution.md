<!-- Copy into a new product's root as CONSTITUTION.md (the CLI's `init` command does this
     automatically). This is the bare L0+L1 skeleton a fresh constitution starts from. Fill
     in L0 via the `define-preamble` skill (never hand-write the vision); harvest L1 once
     L0 is ratified via `harvest-articles`. Never invent Articles to fill the section. -->

# <PROJECT_NAME> Constitution

```
framework: constitution@<FRAMEWORK_VERSION>
ratifier:  <RATIFIER_NAME>
```

## L0 — Preamble (vision)
**P1.** Edit this file to add your product's vision, or run `define-preamble` to elicit
it properly (a human ratifies; an agent may phrase, never originate).

## L1 — Articles (meta-invariants)
*(Empty until harvested. See `.constitution/templates/article.md` for the shape, or run
`harvest-articles` once L0 above is ratified — never invent an Article to fill this section.)*
